import React, { Component } from 'react';
import { Text, View, Platform, AsyncStorage } from 'react-native';
const Price = (props) =>{
    return (<Text>Hello from price</Text>)
}
export default Price;